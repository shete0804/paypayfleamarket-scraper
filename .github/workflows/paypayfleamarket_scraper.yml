name: PayPay Flea Market Scraper

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install Chrome
        run: |
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Configure DNS
        run: |
          echo "Configuring DNS with systemd-resolved..."
          sudo mkdir -p /etc/systemd/resolved.conf.d/
          cat <<'EOF' | sudo tee /etc/systemd/resolved.conf.d/99-custom-dns.conf
          [Resolve]
          DNS=8.8.8.8 1.1.1.1 9.9.9.9
          FallbackDNS=8.8.4.4 1.0.0.1
          EOF
          sudo systemctl restart systemd-resolved
          echo "Testing DNS resolution..."
          nslookup www.paypayfleamarket.yahoo.co.jp || true
          cat /etc/resolv.conf

      - name: Install dependencies
        run: pip install -r paypayfleamarket_requirements.txt

      - name: Run scraper
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python paypayfleamarket_scraper.py